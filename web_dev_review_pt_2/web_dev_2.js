// How can we check that our JS file is linked to our HTML file?


// Utilizing the function and array below, how can we return a list of all of the even numbers?

const testArray = [0, 1, 2, 3, 4, 5]

function getEvens(){

}

// How do we call a function?
// How can we get the return to appear in our JS Chrome Console?

// We want to add a click event to each of our headers that raises an alert with the phrase "THIS IS A HEADER Y'ALL!"

    // What query selector do we need?
    // What can we do to iterate through all of the elements?
    // How can we convert our elements into an array?
    // What is .forEach()?